{% set name = "pyNSID" %}
{% set version = "0.0.4" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.io/packages/source/{{ name[0] }}/{{ name }}/{{ name }}-{{ version }}.tar.gz
  sha256: da77c508e2ef73699ff38400585b2ad110ccdbd19c2f582e0a9afe9da7a0c742

build:
  noarch: python
  number: 0
  script: {{ PYTHON }} -m pip install . -vv

requirements:
  host:
    - python >=3
    - pip
    - pytest-runner
  run:
    - python >=3
    - cytoolz
    - dask-core >=0.10
    - h5py >=2.6.0
    - numpy >=1.10
    - sidpy >=0.0.2
    - six
    - toolz

test:
  imports:
    - pyNSID

about:
  home: https://github.com/pycroscopy/pyNSID
  license: MIT
  license_family: MIT
  license_file: LICENSE
  summary: Framework for storing, visualizing, and processing N-Dimensional Spectroscopic and Imaging Data (NSID)
  doc_url: https://pycroscopy.github.io/pyNSID/
  dev_url: https://github.com/pycroscopy/pyNSID

extra:
  recipe-maintainers:
    - gduscher
    - ssomnath
    - ramav87
